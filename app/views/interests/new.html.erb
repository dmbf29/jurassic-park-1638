<div class="container mt-3">
  <h1>Add Hobby for <%= @dinosaur.name %></h1>
  <%= simple_form_for [@dinosaur, @interest] do |f| %>
    <%= f.association :hobby, collection: @hobbies, prompt: 'Select Hobby' %>
    <%= f.submit 'Add', class: 'btn btn-danger' %>
  <% end %>
</div>
